// EXPORT DEFAULT
/**
 * Exporta un solo elemento predeterminado 
 * de un módulo. Al exportar algo como default, 
 * no necesitas usar el nombre exacto al importarlo
 * en otro archivo; puedes renombrarlo libremente.
 */

export default function multiplicar(a: number, b: number): number {
    return a * b;
}
